$kind: http-request
url: "{{baseUrl}}/api/v1/categories"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("List categories success", function () {
        pm.expect(pm.response.code).to.be.oneOf([200, 204]);
      });
    language: text/javascript
order: 2000
